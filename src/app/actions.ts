'use server';

// AI Tutor functionality is no longer relevant in the new game mode.
// This file can be removed or repurposed later if new AI features are desired.

export async function getAITutorSuggestion(input: any): Promise<string> {
  // This function is no longer used.
  return Promise.resolve('');
}
